# Base path for deploying the Vite app (defaults to "/" when unset)
VITE_APP_BASE_PATH="/"

# Example: API host for downstream services (do not include secrets)
VITE_API_BASE_URL="https://api.example.com"
